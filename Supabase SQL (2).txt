-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.access_grants (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  order_item_id uuid,
  customer_id uuid,
  product_id uuid,
  digital_file_id uuid,
  delivery_method ARRAY,
  delivered_at timestamp with time zone,
  delivery_success boolean,
  delivery_failure_reason text,
  download_count integer DEFAULT 0,
  download_limit integer,
  download_expiry_at timestamp with time zone,
  first_access_at timestamp with time zone,
  last_access_at timestamp with time zone,
  file_viewed boolean DEFAULT false,
  access_revoked boolean DEFAULT false,
  revoked_at timestamp with time zone,
  revoked_by uuid,
  expires_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT access_grants_pkey PRIMARY KEY (id),
  CONSTRAINT access_grants_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT access_grants_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT access_grants_digital_file_id_fkey FOREIGN KEY (digital_file_id) REFERENCES public.digital_files(id),
  CONSTRAINT access_grants_revoked_by_fkey FOREIGN KEY (revoked_by) REFERENCES public.staff_users(id),
  CONSTRAINT fk_access_grants_order FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT fk_access_grants_order_item FOREIGN KEY (order_item_id) REFERENCES public.order_items(id)
);
CREATE TABLE public.analytics_events (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid,
  session_id text,
  event_name text NOT NULL,
  event_timestamp timestamp with time zone DEFAULT now(),
  page_url text,
  referrer text,
  utm_source text,
  utm_medium text,
  utm_campaign text,
  device text,
  browser text,
  country text,
  ip_address inet,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT analytics_events_pkey PRIMARY KEY (id),
  CONSTRAINT analytics_events_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.customers(id)
);
CREATE TABLE public.audit_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  actor_type USER-DEFINED NOT NULL,
  actor_id uuid,
  action text NOT NULL,
  entity_type text NOT NULL,
  entity_id uuid,
  old_value jsonb,
  new_value jsonb,
  ip_address inet,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT audit_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.auth_rate_limits (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  action text NOT NULL,
  bucket_key text NOT NULL,
  window_starts_at timestamp with time zone NOT NULL,
  hit_count integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT auth_rate_limits_pkey PRIMARY KEY (id)
);
CREATE TABLE public.bundles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  included_products ARRAY NOT NULL DEFAULT '{}'::uuid[],
  bundle_price numeric NOT NULL,
  savings_amount numeric,
  bundle_rules jsonb DEFAULT '{}'::jsonb,
  is_visible boolean DEFAULT true,
  delivery_rules jsonb DEFAULT '{}'::jsonb,
  download_structure jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT bundles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.cart_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  cart_id uuid,
  product_id uuid,
  variant_id uuid,
  quantity integer NOT NULL DEFAULT 1,
  unit_price numeric NOT NULL,
  compare_price numeric,
  discounted_price numeric,
  line_total numeric NOT NULL,
  bundle_id uuid,
  added_at timestamp with time zone DEFAULT now(),
  CONSTRAINT cart_items_pkey PRIMARY KEY (id),
  CONSTRAINT cart_items_cart_id_fkey FOREIGN KEY (cart_id) REFERENCES public.carts(id),
  CONSTRAINT cart_items_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT cart_items_variant_id_fkey FOREIGN KEY (variant_id) REFERENCES public.product_variants(id),
  CONSTRAINT cart_items_bundle_id_fkey FOREIGN KEY (bundle_id) REFERENCES public.bundles(id)
);
CREATE TABLE public.carts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  status USER-DEFINED DEFAULT 'active'::cart_status_enum,
  currency text DEFAULT 'INR'::text,
  subtotal numeric DEFAULT 0,
  total numeric DEFAULT 0,
  checkout_started boolean DEFAULT false,
  checkout_started_at timestamp with time zone,
  abandonment_timestamp timestamp with time zone,
  recovery_token text UNIQUE,
  recovery_url text,
  reminder_count integer DEFAULT 0,
  reminder_timestamps ARRAY DEFAULT '{}'::timestamp with time zone[],
  last_reminder_channel text,
  recovered boolean DEFAULT false,
  recovered_order_id uuid,
  incentive_offered text,
  incentive_used boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT carts_pkey PRIMARY KEY (id),
  CONSTRAINT carts_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT fk_carts_recovered_order FOREIGN KEY (recovered_order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.categories (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  parent_id uuid,
  description text,
  image_url text,
  sort_order integer DEFAULT 0,
  seo_title text,
  seo_description text,
  is_featured boolean DEFAULT false,
  is_visible boolean DEFAULT true,
  visibility_rules jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT categories_pkey PRIMARY KEY (id),
  CONSTRAINT categories_parent_id_fkey FOREIGN KEY (parent_id) REFERENCES public.categories(id)
);
CREATE TABLE public.category_featured_products (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  category_id uuid,
  product_id uuid,
  list_type text NOT NULL CHECK (list_type = ANY (ARRAY['featured'::text, 'best_seller'::text, 'new_arrival'::text, 'trending'::text])),
  sort_order integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT category_featured_products_pkey PRIMARY KEY (id),
  CONSTRAINT category_featured_products_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id),
  CONSTRAINT category_featured_products_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.checkout_locks (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  checkout_session_id uuid,
  locked boolean DEFAULT true,
  locked_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone,
  CONSTRAINT checkout_locks_pkey PRIMARY KEY (id)
);
CREATE TABLE public.checkout_sessions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  cart_id uuid,
  customer_id uuid,
  guest_email text,
  guest_phone text,
  billing_address_snapshot jsonb DEFAULT '{}'::jsonb,
  shipping_address_snapshot jsonb DEFAULT '{}'::jsonb,
  tax_snapshot jsonb DEFAULT '{}'::jsonb,
  coupon_snapshot jsonb DEFAULT '{}'::jsonb,
  order_summary_snapshot jsonb DEFAULT '{}'::jsonb,
  payment_method_selected text,
  status USER-DEFINED DEFAULT 'started'::checkout_status_enum,
  device_info text,
  browser_info text,
  recovery_email_sent boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT checkout_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT checkout_sessions_cart_id_fkey FOREIGN KEY (cart_id) REFERENCES public.carts(id),
  CONSTRAINT checkout_sessions_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.conversation_messages (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  conversation_id uuid,
  sender_type USER-DEFINED NOT NULL,
  sender_id uuid,
  content text,
  attachments ARRAY DEFAULT '{}'::text[],
  template_used text,
  delivery_status text DEFAULT 'sent'::text,
  is_read boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT conversation_messages_pkey PRIMARY KEY (id),
  CONSTRAINT conversation_messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.conversations(id)
);
CREATE TABLE public.conversations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  channel text NOT NULL CHECK (channel = ANY (ARRAY['website'::text, 'whatsapp'::text, 'instagram'::text, 'messenger'::text, 'email'::text])),
  assigned_staff_id uuid,
  status text DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'closed'::text, 'pending'::text])),
  last_message_at timestamp with time zone,
  last_reply_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT conversations_pkey PRIMARY KEY (id),
  CONSTRAINT conversations_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT conversations_assigned_staff_id_fkey FOREIGN KEY (assigned_staff_id) REFERENCES public.staff_users(id)
);
CREATE TABLE public.coupons (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  code text NOT NULL UNIQUE,
  coupon_type USER-DEFINED NOT NULL,
  discount_value numeric NOT NULL,
  max_discount_amount numeric,
  min_cart_value numeric DEFAULT 0,
  valid_from timestamp with time zone,
  valid_until timestamp with time zone,
  usage_limit_total integer,
  usage_limit_per_customer integer DEFAULT 1,
  current_usage_count integer DEFAULT 0,
  applicable_products ARRAY DEFAULT '{}'::uuid[],
  applicable_categories ARRAY DEFAULT '{}'::uuid[],
  applicable_segments ARRAY DEFAULT '{}'::text[],
  excluded_products ARRAY DEFAULT '{}'::uuid[],
  excluded_customers ARRAY DEFAULT '{}'::uuid[],
  stackable boolean DEFAULT false,
  first_order_only boolean DEFAULT false,
  new_user_only boolean DEFAULT false,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT coupons_pkey PRIMARY KEY (id)
);
CREATE TABLE public.custom_field_definitions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  entity_type text NOT NULL CHECK (entity_type = ANY (ARRAY['customer'::text, 'product'::text, 'order'::text, 'ticket'::text, 'invoice'::text])),
  field_key text NOT NULL,
  field_label text NOT NULL,
  field_type text NOT NULL CHECK (field_type = ANY (ARRAY['text'::text, 'number'::text, 'boolean'::text, 'date'::text, 'select'::text, 'multi_select'::text, 'json'::text])),
  options jsonb DEFAULT '{}'::jsonb,
  is_required boolean DEFAULT false,
  sort_order integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT custom_field_definitions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.customer_addresses (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  full_name text,
  company_name text,
  phone_number text,
  email text,
  address_line1 text NOT NULL,
  address_line2 text,
  landmark text,
  city text NOT NULL,
  district text,
  state text NOT NULL,
  postal_code text NOT NULL,
  country text NOT NULL DEFAULT 'India'::text,
  is_default boolean DEFAULT false,
  verified boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT customer_addresses_pkey PRIMARY KEY (id),
  CONSTRAINT customer_addresses_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customer_auth_identities (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  provider text NOT NULL CHECK (provider = ANY (ARRAY['email'::text, 'phone'::text])),
  provider_value text NOT NULL,
  normalized_value text NOT NULL,
  verified_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT customer_auth_identities_pkey PRIMARY KEY (id),
  CONSTRAINT customer_auth_identities_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customer_otp_challenges (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid,
  purpose text NOT NULL CHECK (purpose = ANY (ARRAY['account_access'::text, 'guest_checkout'::text])),
  channel text NOT NULL CHECK (channel = ANY (ARRAY['email'::text, 'whatsapp'::text])),
  target text NOT NULL,
  target_normalized text NOT NULL,
  otp_hash text NOT NULL,
  requested_ip text,
  requested_user_agent text,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  attempts integer NOT NULL DEFAULT 0,
  max_attempts integer NOT NULL DEFAULT 5,
  expires_at timestamp with time zone NOT NULL,
  verified_at timestamp with time zone,
  consumed_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT customer_otp_challenges_pkey PRIMARY KEY (id),
  CONSTRAINT customer_otp_challenges_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customer_sessions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  customer_id uuid NOT NULL,
  token_hash text NOT NULL UNIQUE,
  auth_method text NOT NULL CHECK (auth_method = ANY (ARRAY['email_otp'::text, 'whatsapp_otp'::text, 'google_oauth'::text, 'github_oauth'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  last_seen_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  expires_at timestamp with time zone NOT NULL,
  CONSTRAINT customer_sessions_pkey PRIMARY KEY (id),
  CONSTRAINT customer_sessions_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.customers (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  user_id uuid UNIQUE,
  first_name text,
  last_name text,
  full_name text DEFAULT ((first_name || ' '::text) || last_name),
  email text NOT NULL UNIQUE,
  email_verified boolean DEFAULT false,
  phone text,
  phone_verified boolean DEFAULT false,
  password_hash text,
  profile_image text,
  gender text,
  date_of_birth date,
  customer_segment text,
  total_orders integer DEFAULT 0,
  total_spend numeric DEFAULT 0,
  average_order_value numeric DEFAULT 0,
  lifetime_value numeric DEFAULT 0,
  refund_count integer DEFAULT 0,
  chargeback_count integer DEFAULT 0,
  risk_score numeric DEFAULT 0,
  loyalty_tier text,
  reward_points integer DEFAULT 0,
  referral_code text UNIQUE,
  referred_by uuid,
  favorite_categories ARRAY DEFAULT '{}'::uuid[],
  saved_interests ARRAY DEFAULT '{}'::text[],
  notification_prefs jsonb DEFAULT '{}'::jsonb,
  review_reminder_pref boolean DEFAULT true,
  cart_reminder_pref boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT customers_pkey PRIMARY KEY (id),
  CONSTRAINT customers_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT customers_referred_by_fkey FOREIGN KEY (referred_by) REFERENCES public.customers(id)
);
CREATE TABLE public.digital_files (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid,
  file_name text NOT NULL,
  file_type text,
  file_size bigint,
  file_url text NOT NULL,
  file_version text DEFAULT '1.0'::text,
  upload_date timestamp with time zone DEFAULT now(),
  access_permissions jsonb DEFAULT '{}'::jsonb,
  download_limit integer,
  download_expiry_seconds integer,
  storage_path text,
  bucket text,
  mime_type text,
  upload_source text,
  owner_staff_id uuid,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT digital_files_pkey PRIMARY KEY (id),
  CONSTRAINT digital_files_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT digital_files_owner_staff_id_fkey FOREIGN KEY (owner_staff_id) REFERENCES public.staff_users(id)
);
CREATE TABLE public.faqs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  question text NOT NULL,
  answer text NOT NULL,
  category text,
  tags ARRAY,
  seo_slug text UNIQUE,
  is_featured boolean DEFAULT false,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT faqs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.fraud_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  phone text,
  ip_address inet,
  risk_score numeric,
  reason text,
  metadata jsonb,
  blocked boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT fraud_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.gift_cards (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  gift_code text NOT NULL UNIQUE,
  balance numeric NOT NULL,
  original_amount numeric NOT NULL,
  expiry_date timestamp with time zone,
  issued_to uuid,
  issued_by uuid,
  redeemed_by uuid,
  redeemed_at timestamp with time zone,
  status USER-DEFINED DEFAULT 'active'::gift_card_status_enum,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT gift_cards_pkey PRIMARY KEY (id),
  CONSTRAINT gift_cards_issued_to_fkey FOREIGN KEY (issued_to) REFERENCES public.customers(id),
  CONSTRAINT gift_cards_issued_by_fkey FOREIGN KEY (issued_by) REFERENCES public.staff_users(id),
  CONSTRAINT gift_cards_redeemed_by_fkey FOREIGN KEY (redeemed_by) REFERENCES public.customers(id)
);
CREATE TABLE public.help_article_feedback (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  article_id uuid,
  is_helpful boolean NOT NULL,
  user_id uuid,
  session_id text,
  feedback_text text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT help_article_feedback_pkey PRIMARY KEY (id),
  CONSTRAINT help_article_feedback_article_id_fkey FOREIGN KEY (article_id) REFERENCES public.help_articles(id)
);
CREATE TABLE public.help_article_relations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  article_id uuid,
  related_article_id uuid,
  relation_type text DEFAULT 'related'::text,
  sort_order integer DEFAULT 0,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT help_article_relations_pkey PRIMARY KEY (id),
  CONSTRAINT help_article_relations_article_id_fkey FOREIGN KEY (article_id) REFERENCES public.help_articles(id),
  CONSTRAINT help_article_relations_related_article_id_fkey FOREIGN KEY (related_article_id) REFERENCES public.help_articles(id)
);
CREATE TABLE public.help_articles (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  title text NOT NULL,
  slug text NOT NULL UNIQUE,
  content text NOT NULL,
  category text,
  tags ARRAY DEFAULT '{}'::text[],
  seo_title text,
  seo_description text,
  keywords ARRAY,
  is_published boolean DEFAULT true,
  view_count integer DEFAULT 0,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT help_articles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.homepage_sections (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  section_key text NOT NULL UNIQUE,
  product_ids ARRAY DEFAULT '{}'::uuid[],
  is_active boolean DEFAULT true,
  sort_order integer DEFAULT 0,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT homepage_sections_pkey PRIMARY KEY (id)
);
CREATE TABLE public.idempotency_keys (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  key text NOT NULL UNIQUE,
  endpoint text NOT NULL,
  request_hash text,
  response jsonb,
  status text DEFAULT 'processing'::text,
  created_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone,
  CONSTRAINT idempotency_keys_pkey PRIMARY KEY (id)
);
CREATE TABLE public.inventory_reservations (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  variant_id uuid,
  quantity integer NOT NULL,
  reserved_for text,
  status text DEFAULT 'active'::text,
  expires_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT inventory_reservations_pkey PRIMARY KEY (id)
);
CREATE TABLE public.invoice_line_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  invoice_id uuid,
  description text,
  product_name text,
  quantity integer NOT NULL DEFAULT 1,
  unit_price numeric NOT NULL,
  tax_rate numeric DEFAULT 0,
  tax_amount numeric DEFAULT 0,
  total numeric NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT invoice_line_items_pkey PRIMARY KEY (id),
  CONSTRAINT invoice_line_items_invoice_id_fkey FOREIGN KEY (invoice_id) REFERENCES public.invoices(id)
);
CREATE TABLE public.invoices (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  invoice_number text NOT NULL DEFAULT ('INV-'::text || lpad((nextval('invoice_number_seq'::regclass))::text, 6, '0'::text)) UNIQUE,
  customer_id uuid,
  order_id uuid,
  quote_id uuid,
  invoice_status USER-DEFINED DEFAULT 'draft'::invoice_status_enum,
  issue_date timestamp with time zone DEFAULT now(),
  due_date timestamp with time zone,
  currency text DEFAULT 'INR'::text,
  tax_breakdown jsonb DEFAULT '{}'::jsonb,
  subtotal numeric DEFAULT 0,
  discount numeric DEFAULT 0,
  total numeric DEFAULT 0,
  billing_address_snap jsonb DEFAULT '{}'::jsonb,
  gstin text,
  pdf_url text,
  created_by uuid,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT invoices_pkey PRIMARY KEY (id),
  CONSTRAINT invoices_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT invoices_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT invoices_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.staff_users(id)
);
CREATE TABLE public.job_queue (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  job_type text NOT NULL,
  payload jsonb NOT NULL,
  status text DEFAULT 'pending'::text,
  attempts integer DEFAULT 0,
  max_attempts integer DEFAULT 5,
  next_run_at timestamp with time zone DEFAULT now(),
  last_error text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT job_queue_pkey PRIMARY KEY (id)
);
CREATE TABLE public.leads (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text,
  email text,
  phone text,
  company text,
  source text,
  campaign text,
  interested_product uuid,
  estimated_budget numeric,
  lead_stage USER-DEFINED DEFAULT 'new'::lead_stage_enum,
  assigned_staff_id uuid,
  lead_score integer DEFAULT 0,
  notes text,
  last_contacted_at timestamp with time zone,
  next_follow_up timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT leads_pkey PRIMARY KEY (id),
  CONSTRAINT leads_interested_product_fkey FOREIGN KEY (interested_product) REFERENCES public.products(id),
  CONSTRAINT leads_assigned_staff_id_fkey FOREIGN KEY (assigned_staff_id) REFERENCES public.staff_users(id)
);
CREATE TABLE public.notification_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  channel text NOT NULL CHECK (channel = ANY (ARRAY['email'::text, 'sms'::text, 'whatsapp'::text, 'push'::text])),
  type text NOT NULL,
  subject text,
  body text,
  status text DEFAULT 'sent'::text CHECK (status = ANY (ARRAY['sent'::text, 'delivered'::text, 'failed'::text, 'bounced'::text, 'opened'::text, 'clicked'::text])),
  related_order_id uuid,
  related_ticket_id uuid,
  sent_at timestamp with time zone DEFAULT now(),
  delivered_at timestamp with time zone,
  opened_at timestamp with time zone,
  clicked_at timestamp with time zone,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT notification_logs_pkey PRIMARY KEY (id),
  CONSTRAINT notification_logs_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT notification_logs_related_order_id_fkey FOREIGN KEY (related_order_id) REFERENCES public.orders(id),
  CONSTRAINT notification_logs_related_ticket_id_fkey FOREIGN KEY (related_ticket_id) REFERENCES public.support_tickets(id)
);
CREATE TABLE public.order_events (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  event_type text NOT NULL,
  actor USER-DEFINED NOT NULL DEFAULT 'system'::actor_enum,
  actor_id uuid,
  notes text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT order_events_pkey PRIMARY KEY (id),
  CONSTRAINT order_events_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.order_items (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  product_id_snapshot uuid,
  product_name_snapshot text NOT NULL,
  variant_id_snapshot uuid,
  variant_name_snapshot text,
  sku_snapshot text,
  quantity integer NOT NULL DEFAULT 1,
  unit_price numeric NOT NULL,
  compare_price numeric,
  discount_amount numeric DEFAULT 0,
  tax_amount numeric DEFAULT 0,
  line_total numeric NOT NULL,
  product_type_snapshot USER-DEFINED,
  delivery_method_snapshot ARRAY,
  access_duration_snapshot text,
  license_tier_snapshot text,
  download_entitlement_snap jsonb DEFAULT '{}'::jsonb,
  is_refundable boolean DEFAULT true,
  fulfillment_status USER-DEFINED DEFAULT 'unfulfilled'::fulfillment_status_enum,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT order_items_pkey PRIMARY KEY (id),
  CONSTRAINT order_items_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.orders (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_number text NOT NULL DEFAULT ('ORD-'::text || lpad((nextval('order_number_seq'::regclass))::text, 6, '0'::text)) UNIQUE,
  customer_id uuid,
  guest_customer_data jsonb DEFAULT '{}'::jsonb,
  order_status USER-DEFINED NOT NULL DEFAULT 'pending'::order_status_enum,
  payment_status USER-DEFINED NOT NULL DEFAULT 'pending'::payment_status_enum,
  fulfillment_status USER-DEFINED NOT NULL DEFAULT 'unfulfilled'::fulfillment_status_enum,
  delivery_status USER-DEFINED NOT NULL DEFAULT 'pending'::delivery_status_enum,
  source_channel USER-DEFINED NOT NULL DEFAULT 'website'::source_channel_enum,
  currency text NOT NULL DEFAULT 'INR'::text,
  exchange_rate numeric DEFAULT 1,
  subtotal numeric NOT NULL DEFAULT 0,
  discount_total numeric DEFAULT 0,
  tax_total numeric DEFAULT 0,
  shipping_total numeric DEFAULT 0,
  fees_total numeric DEFAULT 0,
  grand_total numeric NOT NULL DEFAULT 0,
  name_snapshot text,
  email_snapshot text,
  phone_snapshot text,
  billing_address_snapshot jsonb DEFAULT '{}'::jsonb,
  shipping_address_snapshot jsonb DEFAULT '{}'::jsonb,
  tax_id_snapshot text,
  company_snapshot text,
  order_notes text,
  order_tags ARRAY DEFAULT '{}'::text[],
  custom_fields jsonb DEFAULT '{}'::jsonb,
  purchase_date timestamp with time zone DEFAULT now(),
  completion_date timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT orders_pkey PRIMARY KEY (id),
  CONSTRAINT orders_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.payment_attempts (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  payment_id uuid,
  checkout_id uuid,
  ip_address inet,
  attempt_timestamp timestamp with time zone DEFAULT now(),
  success boolean DEFAULT false,
  error_logs jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT payment_attempts_pkey PRIMARY KEY (id),
  CONSTRAINT payment_attempts_payment_id_fkey FOREIGN KEY (payment_id) REFERENCES public.payments(id),
  CONSTRAINT payment_attempts_checkout_id_fkey FOREIGN KEY (checkout_id) REFERENCES public.checkout_sessions(id)
);
CREATE TABLE public.payments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  customer_id uuid,
  payment_provider text NOT NULL,
  provider_payment_id text,
  provider_order_id text,
  payment_method text,
  payment_status USER-DEFINED NOT NULL DEFAULT 'pending'::payment_status_enum,
  paid_amount numeric NOT NULL,
  currency text NOT NULL DEFAULT 'INR'::text,
  payment_timestamp timestamp with time zone,
  failure_reason text,
  gateway_response jsonb DEFAULT '{}'::jsonb,
  fraud_risk_result jsonb DEFAULT '{}'::jsonb,
  verification_status text DEFAULT 'unverified'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT payments_pkey PRIMARY KEY (id),
  CONSTRAINT payments_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT payments_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.product_media (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid,
  url text NOT NULL,
  media_type text NOT NULL DEFAULT 'image'::text CHECK (media_type = ANY (ARRAY['image'::text, 'video'::text])),
  alt_text text,
  sort_order integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT product_media_pkey PRIMARY KEY (id),
  CONSTRAINT product_media_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.product_review_media (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  review_id uuid,
  media_url text NOT NULL,
  media_type text CHECK (media_type = ANY (ARRAY['image'::text, 'video'::text])),
  sort_order integer DEFAULT 0,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT product_review_media_pkey PRIMARY KEY (id),
  CONSTRAINT product_review_media_review_id_fkey FOREIGN KEY (review_id) REFERENCES public.product_reviews(id)
);
CREATE TABLE public.product_reviews (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid,
  customer_id uuid,
  order_id uuid,
  rating integer NOT NULL CHECK (rating >= 1 AND rating <= 5),
  title text,
  review_text text,
  is_verified_purchase boolean DEFAULT false,
  helpful_count integer DEFAULT 0,
  not_helpful_count integer DEFAULT 0,
  status text DEFAULT 'published'::text,
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  is_visible boolean NOT NULL DEFAULT true,
  CONSTRAINT product_reviews_pkey PRIMARY KEY (id),
  CONSTRAINT product_reviews_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT product_reviews_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT product_reviews_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id)
);
CREATE TABLE public.product_variants (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  product_id uuid,
  variant_name text NOT NULL,
  sku text UNIQUE,
  price numeric,
  compare_at_price numeric,
  stock_quantity integer DEFAULT 0,
  unlimited_stock boolean DEFAULT true,
  attributes jsonb DEFAULT '{}'::jsonb,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT product_variants_pkey PRIMARY KEY (id),
  CONSTRAINT product_variants_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id)
);
CREATE TABLE public.products (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  name text NOT NULL,
  slug text NOT NULL UNIQUE,
  product_type USER-DEFINED NOT NULL DEFAULT 'digital'::product_type_enum,
  status USER-DEFINED NOT NULL DEFAULT 'draft'::product_status_enum,
  short_description text,
  full_description text,
  brand text,
  publisher text,
  category_id uuid,
  tags ARRAY DEFAULT '{}'::text[],
  base_price numeric NOT NULL DEFAULT 0,
  compare_at_price numeric,
  tax_class text,
  discount_eligible boolean DEFAULT true,
  bundle_price numeric,
  sku text UNIQUE,
  stock_quantity integer DEFAULT 0,
  unlimited_stock boolean DEFAULT true,
  reserved_stock integer DEFAULT 0,
  purchase_limit integer,
  activation_limit integer,
  release_date timestamp with time zone,
  expiry_date timestamp with time zone,
  seo_title text,
  seo_description text,
  requirements jsonb DEFAULT '{}'::jsonb,
  compatibility jsonb DEFAULT '{}'::jsonb,
  custom_fields jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  avg_rating numeric DEFAULT 0,
  total_reviews integer DEFAULT 0,
  CONSTRAINT products_pkey PRIMARY KEY (id),
  CONSTRAINT products_category_id_fkey FOREIGN KEY (category_id) REFERENCES public.categories(id)
);
CREATE TABLE public.promotion_rules (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  coupon_id uuid,
  trigger_condition jsonb DEFAULT '{}'::jsonb,
  auto_apply boolean DEFAULT false,
  campaign_name text,
  priority integer DEFAULT 0,
  schedule jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT promotion_rules_pkey PRIMARY KEY (id),
  CONSTRAINT promotion_rules_coupon_id_fkey FOREIGN KEY (coupon_id) REFERENCES public.coupons(id)
);
CREATE TABLE public.rate_limits (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  identifier text NOT NULL,
  type text NOT NULL,
  request_count integer DEFAULT 0,
  window_start timestamp with time zone NOT NULL,
  window_end timestamp with time zone NOT NULL,
  blocked_until timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT rate_limits_pkey PRIMARY KEY (id)
);
CREATE TABLE public.refunds (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  order_id uuid,
  payment_id uuid,
  customer_id uuid,
  refund_amount numeric NOT NULL,
  refund_type USER-DEFINED NOT NULL,
  refund_reason text,
  refund_status USER-DEFINED NOT NULL DEFAULT 'requested'::refund_status_enum,
  requested_at timestamp with time zone DEFAULT now(),
  approved_at timestamp with time zone,
  refunded_at timestamp with time zone,
  approved_by uuid,
  gateway_refund_id text,
  notes text,
  evidence_urls ARRAY DEFAULT '{}'::text[],
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT refunds_pkey PRIMARY KEY (id),
  CONSTRAINT refunds_order_id_fkey FOREIGN KEY (order_id) REFERENCES public.orders(id),
  CONSTRAINT refunds_payment_id_fkey FOREIGN KEY (payment_id) REFERENCES public.payments(id),
  CONSTRAINT refunds_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT refunds_approved_by_fkey FOREIGN KEY (approved_by) REFERENCES public.staff_users(id)
);
CREATE TABLE public.search_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  session_id text,
  query text NOT NULL,
  results_shown integer DEFAULT 0,
  result_clicks integer DEFAULT 0,
  had_results boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT search_logs_pkey PRIMARY KEY (id),
  CONSTRAINT search_logs_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.search_settings (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  popular_searches ARRAY DEFAULT '{}'::text[],
  synonyms jsonb DEFAULT '{}'::jsonb,
  ranking_overrides jsonb DEFAULT '{}'::jsonb,
  boost_scores jsonb DEFAULT '{}'::jsonb,
  typo_corrections jsonb DEFAULT '{}'::jsonb,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT search_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.staff_activity_logs (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  staff_id uuid,
  action text NOT NULL,
  resource text,
  resource_id uuid,
  ip_address inet,
  device_info text,
  notes text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT staff_activity_logs_pkey PRIMARY KEY (id),
  CONSTRAINT staff_activity_logs_staff_id_fkey FOREIGN KEY (staff_id) REFERENCES public.staff_users(id)
);
CREATE TABLE public.staff_permissions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  role USER-DEFINED NOT NULL,
  permission_name text NOT NULL,
  allowed_actions ARRAY DEFAULT '{}'::text[],
  resource_scope ARRAY DEFAULT '{}'::text[],
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT staff_permissions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.staff_users (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  full_name text NOT NULL,
  email text NOT NULL UNIQUE,
  phone text,
  password_hash text,
  role USER-DEFINED NOT NULL DEFAULT 'support'::staff_role_enum,
  department text,
  status text NOT NULL DEFAULT 'active'::text CHECK (status = ANY (ARRAY['active'::text, 'inactive'::text, 'suspended'::text])),
  profile_image text,
  last_login timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT staff_users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.store_policies (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  store_id uuid,
  privacy_policy text,
  refund_policy text,
  terms_and_conditions text,
  shipping_policy text,
  cancellation_policy text,
  license_usage_policy text,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT store_policies_pkey PRIMARY KEY (id),
  CONSTRAINT store_policies_store_id_fkey FOREIGN KEY (store_id) REFERENCES public.store_settings(id)
);
CREATE TABLE public.store_settings (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  store_name text NOT NULL,
  logo_url text,
  support_email text,
  support_phone text,
  whatsapp_number text,
  guest_checkout_enabled boolean DEFAULT true,
  account_creation_required boolean DEFAULT false,
  tax_inclusive_pricing boolean DEFAULT false,
  auto_invoice_generation boolean DEFAULT true,
  default_currency text NOT NULL DEFAULT 'INR'::text,
  timezone text DEFAULT 'Asia/Kolkata'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT store_settings_pkey PRIMARY KEY (id)
);
CREATE TABLE public.support_attachments (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  ticket_id uuid,
  message_id uuid,
  file_name text NOT NULL,
  file_type text,
  file_url text NOT NULL,
  uploaded_by uuid,
  upload_date timestamp with time zone DEFAULT now(),
  CONSTRAINT support_attachments_pkey PRIMARY KEY (id),
  CONSTRAINT support_attachments_ticket_id_fkey FOREIGN KEY (ticket_id) REFERENCES public.support_tickets(id),
  CONSTRAINT support_attachments_message_id_fkey FOREIGN KEY (message_id) REFERENCES public.ticket_messages(id)
);
CREATE TABLE public.support_tickets (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  ticket_number text NOT NULL DEFAULT ('TKT-'::text || lpad((nextval('ticket_number_seq'::regclass))::text, 6, '0'::text)) UNIQUE,
  customer_id uuid,
  guest_email text,
  guest_phone text,
  subject text NOT NULL,
  description text,
  category USER-DEFINED,
  priority USER-DEFINED DEFAULT 'medium'::ticket_priority_enum,
  status USER-DEFINED DEFAULT 'open'::ticket_status_enum,
  assigned_staff_id uuid,
  related_order_id uuid,
  related_payment_id uuid,
  related_product_id uuid,
  channel USER-DEFINED DEFAULT 'email'::ticket_channel_enum,
  sla_deadline timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  closed_at timestamp with time zone,
  CONSTRAINT support_tickets_pkey PRIMARY KEY (id),
  CONSTRAINT support_tickets_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT support_tickets_assigned_staff_id_fkey FOREIGN KEY (assigned_staff_id) REFERENCES public.staff_users(id),
  CONSTRAINT support_tickets_related_order_id_fkey FOREIGN KEY (related_order_id) REFERENCES public.orders(id),
  CONSTRAINT support_tickets_related_payment_id_fkey FOREIGN KEY (related_payment_id) REFERENCES public.payments(id),
  CONSTRAINT support_tickets_related_product_id_fkey FOREIGN KEY (related_product_id) REFERENCES public.products(id)
);
CREATE TABLE public.ticket_messages (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  ticket_id uuid,
  sender_type USER-DEFINED NOT NULL,
  sender_id uuid,
  message_body text NOT NULL,
  attachments ARRAY DEFAULT '{}'::text[],
  is_read boolean DEFAULT false,
  sent_at timestamp with time zone DEFAULT now(),
  edited_at timestamp with time zone,
  deleted boolean DEFAULT false,
  CONSTRAINT ticket_messages_pkey PRIMARY KEY (id),
  CONSTRAINT ticket_messages_ticket_id_fkey FOREIGN KEY (ticket_id) REFERENCES public.support_tickets(id)
);
CREATE TABLE public.user_identities (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  provider text NOT NULL,
  provider_user_id text NOT NULL,
  email text,
  linked_at timestamp without time zone DEFAULT now(),
  provider_data jsonb NOT NULL DEFAULT '{}'::jsonb,
  avatar_url text,
  CONSTRAINT user_identities_pkey PRIMARY KEY (id),
  CONSTRAINT user_identities_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.wallet_transactions (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  wallet_id uuid,
  type USER-DEFINED NOT NULL,
  amount numeric NOT NULL,
  reason text,
  related_order_id uuid,
  related_payment_id uuid,
  related_invoice_id uuid,
  performed_by USER-DEFINED DEFAULT 'system'::actor_enum,
  performed_by_id uuid,
  notes text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wallet_transactions_pkey PRIMARY KEY (id),
  CONSTRAINT wallet_transactions_wallet_id_fkey FOREIGN KEY (wallet_id) REFERENCES public.wallets(id),
  CONSTRAINT wallet_transactions_related_order_id_fkey FOREIGN KEY (related_order_id) REFERENCES public.orders(id),
  CONSTRAINT wallet_transactions_related_payment_id_fkey FOREIGN KEY (related_payment_id) REFERENCES public.payments(id),
  CONSTRAINT wallet_transactions_related_invoice_id_fkey FOREIGN KEY (related_invoice_id) REFERENCES public.invoices(id)
);
CREATE TABLE public.wallets (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid UNIQUE,
  current_balance numeric DEFAULT 0,
  currency text DEFAULT 'INR'::text,
  lifetime_credited numeric DEFAULT 0,
  lifetime_debited numeric DEFAULT 0,
  last_updated timestamp with time zone DEFAULT now(),
  CONSTRAINT wallets_pkey PRIMARY KEY (id),
  CONSTRAINT wallets_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id)
);
CREATE TABLE public.wishlists (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  customer_id uuid,
  product_id uuid,
  variant_id uuid,
  added_at timestamp with time zone DEFAULT now(),
  priority integer DEFAULT 0,
  notes text,
  notify_on_discount boolean DEFAULT false,
  notify_on_back_in_stock boolean DEFAULT false,
  CONSTRAINT wishlists_pkey PRIMARY KEY (id),
  CONSTRAINT wishlists_customer_id_fkey FOREIGN KEY (customer_id) REFERENCES public.customers(id),
  CONSTRAINT wishlists_product_id_fkey FOREIGN KEY (product_id) REFERENCES public.products(id),
  CONSTRAINT wishlists_variant_id_fkey FOREIGN KEY (variant_id) REFERENCES public.product_variants(id)
);